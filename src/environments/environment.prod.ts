export const environment = {
  production: true,
  apiUrl: 'https://your-production-api-domain.com/api', // Replace with your production API URL
  tokenExpirationBuffer: 5 * 60 * 1000, // 5 minutes before expiry
};
